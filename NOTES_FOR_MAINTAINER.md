- `data/papers.yaml`: `cam3d2025` currently has `bibtex_key: null` because no matching key was found in `book/references.bib`. If the monograph or shared citation namespace needs this paper, please add a corresponding BibTeX entry/key and then align `data/papers.yaml`.
