name: one-shot-awesome-discovery-diagnostics-fix

on:
  pull_request:
    branches: [agent/project-audit-remediation]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  fix:
    if: github.head_ref == 'agent/awesome-special-remediation'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/awesome-special-remediation
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install pyyaml black ruff
      - name: Apply diagnostic changes
        run: |
          python scripts/prepare_awesome_discovery_diagnostics.py
          black scripts/discover_awesome.py
          ruff check scripts/discover_awesome.py
          python scripts/discover_awesome.py --self-test
          python scripts/check_awesome.py
      - name: Commit diagnostic changes
        run: |
          rm -f scripts/prepare_awesome_discovery_diagnostics.py
          rm -f .github/workflows/awesome-discovery-diagnostics-fix.yml
          git config user.name "CamoGED Awesome Editorial Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "fix(awesome): preserve live discovery diagnostics"
          git push origin HEAD:agent/awesome-special-remediation
