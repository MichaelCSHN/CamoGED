name: arXiv watch (resource candidates)
# Not yet implemented — run manually only. The weekly schedule is intentionally
# disabled so this placeholder does not run (and do nothing) every week. Re-enable
# the cron trigger once the arXiv query + triage-issue step below is built.
on:
  workflow_dispatch:
jobs:
  watch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: |
          echo "TODO: query arXiv for new camouflage papers and open a triage issue."
          echo "Suggested query: cat:cs.CV AND (camouflage OR concealed)"
